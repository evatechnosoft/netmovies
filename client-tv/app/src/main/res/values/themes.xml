<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose UI'yi barındıran pencere teması — platform Material teması kullanır,
         böylece ekstra appcompat/material XML bağımlılığı gerekmez. -->
    <style name="Theme.NetMoviesTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Widget düğmeleri: dördü eşit ağırlıkta, dokunma hedefi tam yükseklik. -->
    <style name="NmWidgetDugme">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/widget_dugme</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Widget poster: üçü eşit, tam yükseklik, kırpmadan sığdır. -->
    <style name="NmWidgetPoster">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/widget_dugme</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <!-- Pad tuşu: kare gözler, dokunma hedefi hücrenin tamamı. -->
    <style name="NmWidgetPad">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">3dp</item>
        <item name="android:background">@drawable/widget_dugme</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:textSize">17sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
