<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose UI'yi barındıran pencere teması — platform Material teması kullanır,
         böylece ekstra appcompat/material XML bağımlılığı gerekmez. -->
    <style name="Theme.NetMoviesTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
