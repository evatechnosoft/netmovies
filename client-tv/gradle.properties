org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Ev sunucunun (stream servisi) LAN adresi — build sırasında BASE_URL'e gömülür.
# Kendi IP'ni gir; boş bırakırsan app/build.gradle.kts içindeki varsayılan kullanılır.
# Örnek: NETMOVIES_BASE_URL=http://192.168.0.28:3310
NETMOVIES_BASE_URL=
