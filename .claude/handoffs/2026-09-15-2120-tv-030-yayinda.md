# DEVİR — TV 0.3.0 yayında, cihaz denemesi bekliyor

**Zaman:** 15 Eylül 2026, 21:20 · **Dal:** `fix/general-stability` @ `094007c` · **Oturum:** 3178259a
**Kirli:** 1 dosya — `.claude/handoffs/2026-09-04-1610-lan-warp-ota.md` silinmiş (eski devir, geri koyma)
**Push:** netmovies EDİLMEDİ · evaglass-releases @ `0fd4acf` EDİLDİ

## Hedef

Dean'in bu oturumdaki dört isteği, sırayla — hepsi kapandı:
1. "Favoriye aldığım dizilerim oynanabilir kaynak vermedi, nedendir"
2. Oynatıcı: dakika tuşu telefon düzeninde 4x4 ve altta; alt barda oynat/sarma/bölüm
   geçme gezinilebilir, bölüm listesine oradan girilsin, küçük ikonlarla; sarma
   sayfası koca ekranı kaplamasın, sol altta küçük çıksın.
3. "Orası her ortamı taramalı, DiziMom DDizi vb denerken de bilelim — tek yerden
   eklemedim, o dizi olarak favorim."
4. "Yayınla sorma artık bitince geliştirme yayınla."

## Durum

`fcac4db` zincir · `ab0cbf7` arayüz + tarama kapsamı · `094007c` sürüm 0.3.0.

### Doğrulanmış (tool çıktısı var)

| İddia | Kanıt |
|---|---|
| DiziMom favorileri kaynak veriyor | "Daha 17" ve "Altı Üstü İstanbul" 1. ve son bölüm: 0 → **1 kaynak** |
| Gerileme yok | hdplayersystem kullanan diziler 1 → 1 |
| Uçtan uca oynanıyor | proxy master **200** → varyant **200** → segment **200** |
| Tarama kapsamı | `kapsam` teşhisi: **12 sağlayıcı** (DDizi, FullHDFilmizlesene, JetFilmizle, M3UPlaylist artık içeride) |
| Yanlış eşleşme gitti | "Daha 17" → "Hızlı ve Öfkeli 2 Daha Hızlı Daha Öfkeli" eşleşmesi yok |
| Testler | motor `Ran 40 ... OK` · gateway `Ran 132 ... OK` · `:app:testDebugUnitTest` OK |
| Kapı | `bash scripts/smoke.sh` → "SONUÇ: kapı YEŞİL" |
| TV APK | `:app:assembleDebug` EXIT=0 · `NetMovies-TV-v0.3.0.apk` · 20.232.318 bayt |
| Yerel OTA | `app_update?target=tv` → `tag v0.3.0-poc`, 20.232.318 bayt |
| GitHub release | `netmovies-tv-v0.3.0` indirme **http 200**, 20.232.318 bayt |
| Katalog canlı | raw apps.json → netmovies-tv **0.3.0 vc 300**, netmovies-phone aynı |

sha256: `e0a82248478820fd8b411c7770a56f7d98da27b019ee7428193cf31b5ff9cf50`

### DOĞRULANMADI

**Yeni oynatıcı arayüzü televizyonda hiç görülmedi.** Alt bar ve 4x4 tuş takımı yalnız
derlendi ve dex'te dizeleri doğrulandı ("Alt kumanda bar", "Dakika", "Ana sayfa").

## Kök nedenler ve kararlar

**1. FirePlayer embed'inin iki adres biçimi var.** `fireplayer_sources`
(`engine/Plugins/__dizi_common.py`) yalnız `<origin>/player/index.php?...&do=getVideo`
deniyordu. `/tv/` altındaki kurulumlar (peacemakerst.com, hdstreamable.com) orada **404**
verip yalnız sayfanın KENDİSİNE (`<iframe>?do=getVideo`) cevap veriyor; linki de
`securedLink`/`videoSource` yerine `videoSources[].file` içinde döndürüyorlar. İkisi de
denenip üç yanıt biçimi de okunuyor (`_fireplayer_stream`). hdplayersystem kök kurulum
olduğu için çalışıyordu — anime çalışıp yerli dizinin çalışmamasının sebebi buydu.

**2. Proxy referer reddeden CDN'e referer gönderiyordu.** Akış `video.twimg.com`
üzerinden geliyor: referer'sız 200, embed referer'ı ile **403**. Kaynak bulunuyor ama
oynatıcıda "Upstream Error: 403" çıkıyordu. `_REFERER_REDDEDEN_HOSTLAR`
(`stream/Public/Proxy/Libs/helpers.py`) — imzalı adres referer istemiyor.

**3. Alternatif tarama KAPSAMI elle yazılmış listeden geliyordu.** `ALTERNATIVE_ORDER`
9 ad, motorda 16 eklenti yüklü; sıra listesi aynı zamanda kapsamı belirlediği için
DDizi/FullHDFilmizlesene/JetFilmizle/M3UPlaylist zincire hiç girmiyordu. `tum_saglayicilar()`
(`engine/Public/API/v1/Libs/tarama_sirasi.py`) kapsamı yüklü eklentilerden, sırayı yine
puan/öncelik listesinden alıyor; yetişkin eklentileri `TARAMA_DISI`.

**4. Kapsam genişleyince yanlış eşleşme çıktı.** `_anlamli_kelimeler` 2 harften kısa
parçaları atıyordu: "Daha 17" → `{daha}` kalınca alakasız film eşleşme sayılıyordu.
Sayılar uzunluk sınırından muaf + regresyon testi (`engine/tests/test_query_variants.py`).

**5. Arayüz.** `QuickPad` sağ alt dikey kutudan ALT BAR'a döndü; bar açıkken
`ControlsOverlay` çizilmiyor (ikisi de ekranın altına oturuyordu) — süre ve ilerleme
çubuğu bu yüzden barın üstüne taşındı. AŞAĞI tuşu yeni `RemoteAction.OPEN_BAR`'a bağlandı
(eskiden OPEN_SETTINGS'ti; Ayarlar artık barın içinde bir düğme). `SeekScreen` tam ekran +
tek sıra 0-9 iken sol altta 300dp kutu ve telefon düzeni 4x4 oldu.

**6. Yayın kuralı değişti.** "Cihazda denenmemiş sürüm televizyona güncelleme diye
düşmesin" kuralı KALKTI. Dean: "Yayınla sorma artık bitince geliştirme yayınla."
Hafızaya yazıldı: `memory/yayinlamak-icin-sorma.md`. Bundan sonra kanıt yeşilse sürüm
artırılır ve üç dağıtım yeri birden güncellenir; onay sorulmaz, ne yayınlandığı söylenir.

**7. TV APK adı build'de üretiliyor.** `NetMovies-TV-vX.Y.Z.apk` — OTA ucu hedefi
`netmovies-tv-` önekinden, sürümü addaki `vX.Y.Z`den okuyor; `app-debug.apk` her yayında
elle adlandırılıyordu (saat tarafında aynı düzeltme `6b3a622`).

## Tekrarlama — ölen yollar

- **`resolve_sources?...&url=`** diye çağırma: uç `encoded_url` ister, `url` verilince
  **410**. `encoded_url` base64 DEĞİL, **tek kez** yüzde-kodlanmış düz URL (gateway
  `dict(request.query_params)` ile bir kez çözüyor). Base64 verirsen
  "UnsupportedProtocol: Request URL is missing an 'http://'" görürsün — kodda değil,
  çağrıda hata vardır.
- **`fuck_dmca` yanıtı 180 sn cache'li**: düzeltmeden hemen sonra aynı parametreyle
  çağırırsan ESKİ sonucu görürsün. Parametreyi değiştir (`&episode=0&mode=full`) ya da bekle.
- **Bash tool'unda ters bölü yeniyor**: heredoc'a yazılan Python regex'lerinde `\\x` → `\x`
  olup "incomplete escape" veriyor. Python dosyasını `python - <<PY` ile ÜRET, sonra çalıştır.
- **DDizi'yi "bozuk" sanma**: yalnız resmi YouTube yayınlarını çözüyor (`_YOUTUBE` +
  `ytdlp_info`). Dizi sayfasında YouTube yerleşimi yoksa 0 kaynak — tasarım, hata değil.
- **Motor içinde script**: `cd /usr/src/KekikStreamAPI && PYTHONPATH=. python ...` şart;
  `docker exec -w` tek başına `ModuleNotFoundError: Plugins` veriyor.
- Önceki devirlerden geçerli olanlar: `gh release create --target main`; Git Bash'te
  `MSYS_NO_PATHCONV=1 docker exec -w`; Kotlin dosyasını bash heredoc ile yazma.

## SIRADAKİ İŞ #1 — 0.3.0'ı televizyonda dene

Televizyonda evaitecOTA'yı aç → NetMovies 0.3.0 (vc 300) → kur. Sırayla:

1. Favorilerden **Altı Üstü İstanbul** ya da **Daha 17** aç → oynamalı (asıl düzeltme).
2. **AŞAĞI** tuşu → alt bar açılmalı; D-pad ile ⏮ · −5dk · −30sn · ▶ · +30sn · +5dk ·
   ⏭ · Bölümler · Dakika · Ayarlar · Ana sayfa · Kapat arasında gezilmeli. Süre ve
   ilerleme çubuğu barın üstünde görünmeli.
3. Bardan **Dakika** → sol altta 300dp kutu; `1 2 3 ⌫ / 4 5 6 0 / 7 8 9 ▶ / ⏮ ⏭ 📑 ✕`.
   Görüntü kararmamalı. 45 yaz → ▶ → 45. dakikaya gitmeli.
4. `curl -s localhost:3310/api/v1/client_log` → `sunucu·kapsam` satırı denenen
   sağlayıcıların tamamını yazmalı; `ses · tampon boşaldı` hiç olmamalı.

Alt bar açılmıyorsa AŞAĞI eşlemesi değişmiş olabilir: Ayarlar → Buton Eşleme →
Aşağı ▼ → "Alt kumanda barı".

## SIRADAKİ İŞ #2 — saat 0.1.3'ü bilekte dene

`docs/HANDOFF.md` SIRADAKİ İŞ #1'den devralındı, bu oturumda dokunulmadı.

## Açık kalan — Dean'in yarım bıraktığı istek

"İsimlendirme standardı, pipeline/PR klasörleri, environment'ları ayıralım" diye
başlayıp **"Pardon yanlış oldu"** ile iptal etti. Depo kökünde 30+ `NetMovies-TV-*.apk`
duruyor ve `.env.bak*` dosyaları var — konu tekrar açılırsa başlangıç noktası bu.
Kendiliğinden başlama.
